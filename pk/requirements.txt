# Exact versions, so provisioning is reproducible and download volume is
# estimable for the report.
#
# Django 3.2 was originally pinned because bento/ubuntu-18.04 shipped Python
# 3.6. The node is on bento/ubuntu-22.04 (Python 3.10) now, which 3.2 still
# supports, so the pin is left alone -- but it is what keeps this node off
# 24.04, whose Python 3.12 needs Django 4.2 or newer. Upgrading is a
# post-v1 job, not a provisioning one.
Django==3.2.25
gunicorn==20.1.0
